# AUTOPRO Frontend

Next.js (App Router) фронт для сервиса аренды AUTOPRO.

## Запуск

```bash
cd front
npm install
npm run dev
```

По умолчанию фронт ожидает backend по адресу `http://localhost:8000/api/v1`.
Можно переопределить через переменную окружения `NEXT_PUBLIC_API_BASE_URL`.

